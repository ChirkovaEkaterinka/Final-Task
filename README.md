## В финальной работе представлены следующие файлы: 
- задания - файл "Контрольная работа.png";
- две блок-схемы - файлы "Scheme_1_array_set_at_start.png" и "Scheme_2_array_input.png";
- папка с программой для решения поставленной задачи - "Task".

---

**Файл "Контрольная работа"** содержит описание заданий.

**Блок-схема "Scheme_1_array_set_at_start"** для решения поставленной задачи при условии заданного на старте массива.

**Блок-схема "Scheme_2_array_input"** для решения поставленной задачи при условии ввода массива с клавиатуры.

**Папка "Task"** содержит непосредственно программу для решения задачи по указанному в блок-схемах алгоритму и системные файлы. 

---

***Описание алгоритма решения***

Задача: *Написать программу, которая из имеющегося массива строк формирует массив из строк, длина которых меньше
либо равна 3 символа. Первоначальный массив можно ввести с клавиатуры или задать на старте выполнения алгоритма.*

1) Сначала объявляем массив, который пользователь задает с клавиатуры. Для этого используем метод *GetArray* для вводы и метод *PrintArray* для вывода в консоль.
2) Пользуясь методом *ArrayIfThreeOrLess*, который на вход принимает введенный пользователем массив, проверяется условие длина элемента меньше или равна 3, и если условие выполняется, то в новый массив заносится этот элемент под индексом *count*.
3) После присвоения индекса элементу нового массива переменная *count* увеличивается на один и возвращается к циклу for, в котором i увеличивается на один.  
3) Пользуясь базовым методом *Array.Resize* приводим полученный новый массив к count количеству элементов,чтобы не было пробелов, и на выходе метода *ArrayIfThreeOrLess* возвращаем новый массив *newArray*. 
4) Выводим полученный массив *newArray* в консоль снова методом *PrintArray*.
